---
"meta-plugin-creator": patch
---

Add synchronized bilingual READMEs at the repo root: `README.md` (English, canonical) and `README.zh-TW.md` (Traditional Chinese translation), kept structurally in sync — same section order and heading count, with a language-switcher link at the top of each. Both cover what the plugin is, installation via the repo's self-marketplace, usage of the `plugin-dev` skill (init and extend modes), repo structure, development/verification, and license.

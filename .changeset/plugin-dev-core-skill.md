---
"meta-plugin-creator": minor
---

Add the core `plugin-dev` skill: a model-invoked skill that guides plugin-level development of claude-plugins — init and extend modes, a fixed interview, externalized plan state in `.meta-plugin-creator/plan.md`, component authoring delegated to sub-agents via self-contained briefs, and worktree-safe path handling. Ships as the first entry in the plugin.json skills whitelist.
